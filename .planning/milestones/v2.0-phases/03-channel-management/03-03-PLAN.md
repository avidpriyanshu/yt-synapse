---
phase: 03-channel-management
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - logic-engine/main.js
  - obsidian-vault/channels/*.md (generated per channel)
  - obsidian-vault/Channels-Index.md (new)
autonomous: true
requirements: [CHAN-01, CHAN-02, CHAN-03, CHAN-04]
---

<objective>
Auto-generate dedicated channel pages with categorization system for all YouTube channels in the vault.

Purpose: Enable users to browse, categorize, and discover videos organized by creator channel with category tags and chronological listings.

Output: 
- Enhanced channel page generation in scraper (auto-create on new channel encountered)
- Category tagging system in channel frontmatter (user-editable tags field)
- Channel pages with video lists sorted by date (newest first)
- Channels index page for discovery
- Full support for CHAN-01 through CHAN-04 requirements
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md

This plan operates within an existing Obsidian vault with:
- 135 video notes in `/obsidian-vault/videos/` with channel metadata in frontmatter
- Scraper logic in `/logic-engine/main.js` already tracks channels in `channels.json`
- 2 existing channel pages: Tim Gabe.md, TÂCHES TEACHES.md (manually created)
- 10 unique channels identified in channels.json
- Dataview plugin available for dynamic queries in channel pages
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-channel-management/03-CONTEXT.md

## Existing Channel Structure

Current channels.json contains:
```json
{
  "channels": {
    "UC8WLg9gshrtZA1-swL5D9Kg": {
      "feedTitle": "Tim Gabe",
      "sourceVideoId": "...",
      "resolvedAt": "2026-04-19T..."
    },
    ... (10 total)
  }
}
```

## Current Channel Page Template

Existing Tim Gabe.md and TÂCHES TEACHES.md use:
- YAML frontmatter: title, channel_id, type: channel, tags: [youtube/channel]
- YouTube channel link
- Dataview query to list all videos from that channel sorted by date DESC

## Video Frontmatter Structure

Each video note has:
```yaml
channel: [[Channel Name]]
channel_id: YouTube_Channel_ID
date: YYYY-MM-DD
type: video
tags: [youtube/video]
```

## Requirements Mapping

| Req ID | Requirement | Implementation |
|--------|-------------|-----------------|
| CHAN-01 | Auto-generate one page per unique channel | Task 1: Enhance scraper to auto-generate channel pages |
| CHAN-02 | User assign tags/categories to channels | Task 2: Add `tags` field to channel frontmatter template |
| CHAN-03 | Filter/browse by channel category | Task 3: Create Channels Index with categorization view |
| CHAN-04 | Channel page displays chronological videos | Task 4: Verify dataview queries list videos correctly |

</context>

<tasks>

<task type="auto">
  <name>Task 1: Add channel page auto-generation to scraper</name>
  <files>logic-engine/main.js</files>
  <action>
Enhance the main.js scraper to auto-generate channel pages when new channels are encountered:

1. Find the `upsertChannelRecord` function (line ~75) — this is called when new channels are discovered
2. After upserting channel metadata to channels.json, call a new function `generateChannelPage(channelId, channelTitle)` (create stub if needed)
3. Implement `generateChannelPage()` to:
   - Create channel slug: sanitize channel name (same as ReviewAgent.sanitizeFilename)
   - Check if `/obsidian-vault/channels/{slug}.md` exists
   - If NOT exists: Create new file with channel page template
   - If EXISTS: Verify page has required frontmatter (tags field), add if missing
   - Use channel ID from channels.json as source of truth
4. Channel page template should include:
   - Frontmatter: title (channel name), channel_id, type: channel, tags: [] (empty, user fills), creation date
   - Body: YouTube link, description placeholder, dataview query listing videos
5. Dataview query format:
   ```
   TABLE title as "Title", date as "Published", topics as "Topics"
   FROM "videos"
   WHERE channel_id = "{CHANNEL_ID}" OR channel = [[Channel Name]]
   SORT date DESC
   ```
6. Do NOT overwrite existing channel pages (only add missing ones, never delete or modify user edits)

Requirements covered: CHAN-01 (auto-generate pages)
  </action>
  <verify>
    <automated>
grep -l "generateChannelPage" /Users/avid/Desktop/yt-vault/logic-engine/main.js && \
ls /Users/avid/Desktop/yt-vault/obsidian-vault/channels/ | wc -l
    </automated>
  </verify>
  <done>
- generateChannelPage function exists and is called after upsertChannelRecord
- Function creates channel pages for new channels discovered
- Existing pages are not overwritten
- Channel pages have required frontmatter (title, channel_id, type, tags field)
- At least 8 channel pages exist (for 10 unique channels found in scraper history)
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify channel pages have categorization frontmatter</name>
  <files>obsidian-vault/channels/*.md</files>
  <action>
Ensure all channel pages have the `tags` field in YAML frontmatter for user categorization:

1. Scan all existing channel pages in `/obsidian-vault/channels/`
2. For each .md file:
   - Check if `tags:` field exists in frontmatter
   - If missing: Add `tags: []` (empty array, user will fill)
   - If present but not array: Fix to be proper YAML array
3. Suggested tag categories (for user reference in comments):
   - Tech, Design, Product, Education, Entertainment, Science, Lifestyle, Business
4. Example frontmatter after update:
   ```yaml
   ---
   title: "Kole Jain"
   channel_id: UCR6MXpfFWbOpNlxuhWCBV_A
   type: channel
   tags:
     - Design
     - Product
   ---
   ```
5. Do NOT assign tags automatically — leave empty for user to fill
6. Verify all pages are valid YAML with frontmatter properly closed by `---`

Requirements covered: CHAN-02 (enable tag assignment)
  </action>
  <verify>
    <automated>
for f in /Users/avid/Desktop/yt-vault/obsidian-vault/channels/*.md; do
  grep -A1 "^tags:" "$f" || echo "MISSING tags in $f"
done | grep -c "MISSING" || echo "0"
    </automated>
  </verify>
  <done>
- All channel pages have `tags:` field in frontmatter (checked all .md files)
- Tags field is proper YAML (array or empty list)
- No missing frontmatter markers
- All pages are valid YAML (no parse errors)
  </done>
</task>

<task type="auto">
  <name>Task 3: Create Channels Index page for discovery and categorization</name>
  <files>obsidian-vault/Channels-Index.md</files>
  <action>
Create a new index page to help users browse channels by category and see all available channels:

1. Create `/obsidian-vault/Channels-Index.md` (new file)
2. Content structure:
   - Header: "# Channel Directory"
   - Brief description: "Browse all creators organized by category or alphabetically"
   - Section 1: "All Channels (Alphabetical)"
     - Dataview query: List all files tagged with `youtube/channel`, sorted by title
     - Format: `[[Channel Name]]` — simple linked list
   - Section 2: "Browse by Category"
     - Show a Dataview query that groups channels by tag
     - Example output:
       ```
       ## Tech
       - [[Kole Jain]]
       - [[Tim Gabe]]
       
       ## Design
       - [[Naphtali Rosenberg]]
       ```
3. Dataview queries:
   - All channels: `LIST file.link FROM "channels" SORT file.name`
   - By category: `GROUP BY tags FROM "channels"` (or manual section if dynamic grouping not supported)
4. Add navigation hints:
   - "Click a channel name to view all videos from that creator"
   - "Channels can be categorized by editing the `tags` field in each channel page"
5. Keep design simple — rely on Obsidian theme, no custom CSS

Requirements covered: CHAN-03 (browse by category), CHAN-04 (discoverable channel pages)
  </action>
  <verify>
    <automated>
[ -f /Users/avid/Desktop/yt-vault/obsidian-vault/Channels-Index.md ] && \
grep -E "# Channel Directory|Browse|LIST|GROUP BY" /Users/avid/Desktop/yt-vault/obsidian-vault/Channels-Index.md | head -3
    </automated>
  </verify>
  <done>
- Channels-Index.md exists at correct path
- Contains header "Channel Directory"
- Has dataview queries for listing channels (all + by category)
- Has navigation hints for users
- File is valid markdown with proper formatting
  </done>
</task>

<task type="auto">
  <name>Task 4: Verify channel page dataview queries work correctly</name>
  <files>obsidian-vault/channels/*.md</files>
  <action>
Ensure all channel pages have working dataview queries that correctly list videos:

1. Examine 3 sample channel pages (pick channels with multiple videos):
   - Check query format matches expected pattern
   - Verify query uses correct field names (channel_id, channel, date, topics)
2. For each channel page:
   - Query should filter by: `channel_id = "..."` OR `channel = [[...]]` (redundancy for reliability)
   - Sort by: `SORT date DESC` (newest first)
   - Columns shown: title, date, topics
3. Create 1 test: Pick one channel (e.g., "Tim Gabe"), verify:
   - Channel page query filters to videos with matching channel_id
   - At least 3+ videos appear in results
   - Videos sorted by date DESC
4. Update any malformed queries found

Requirements covered: CHAN-04 (display chronological videos)
  </action>
  <verify>
    <automated>
grep -h "WHERE channel_id\|SORT date DESC" /Users/avid/Desktop/yt-vault/obsidian-vault/channels/*.md | wc -l
    </automated>
  </verify>
  <done>
- All channel pages have dataview queries with WHERE clause
- All queries sort by date DESC
- Queries use channel_id for filtering (primary) and channel link as fallback
- Sample validation: Tim Gabe channel shows 3+ videos in correct date order
  </done>
</task>

<task type="auto">
  <name>Task 5: Verify all 10 unique channels have pages</name>
  <files>obsidian-vault/channels/</files>
  <action>
Ensure all channels found in channels.json have corresponding .md pages in vault:

1. Extract all channel IDs and titles from `/logic-engine/channels.json`:
   - Expected: 10 unique channels (from context notes)
2. For each channel in channels.json:
   - Verify a corresponding .md page exists in `/obsidian-vault/channels/`
   - Page name should match sanitized channel name from feedTitle (e.g., "Tim Gabe.md")
   - If missing: Log which channels are missing pages
3. Create missing channel pages if any found:
   - Use same template as existing pages
   - Populate title, channel_id, type, tags fields
   - Include YouTube link and dataview query
4. Count final result: Should have 10 channel pages (one per unique channel)

Requirements covered: CHAN-01 (all channels have pages)
  </action>
  <verify>
    <automated>
echo "Channels in channels.json:" && \
jq '.channels | keys | length' /Users/avid/Desktop/yt-vault/logic-engine/channels.json && \
echo "Channel pages in vault:" && \
ls /Users/avid/Desktop/yt-vault/obsidian-vault/channels/*.md | wc -l
    </automated>
  </verify>
  <done>
- channels.json has 10 unique channels
- /obsidian-vault/channels/ has 10 .md files (one per channel)
- All pages have required frontmatter (title, channel_id, type, tags)
- All pages have dataview queries for listing videos
- All pages are valid markdown files
  </done>
</task>

<task type="auto">
  <name>Task 6: Add Channel Management documentation</name>
  <files>obsidian-vault/CHANNEL-MANAGEMENT.md</files>
  <action>
Create user documentation for the channel management system:

1. Create `/obsidian-vault/CHANNEL-MANAGEMENT.md` (new file)
2. Content:
   - Header: "# Channel Management Guide"
   - Section: "How to Browse Channels"
     - Link to Channels-Index.md
     - Explanation: "Each channel has its own page showing all videos from that creator"
   - Section: "Categorizing Channels"
     - Explain how to edit `tags` field in channel frontmatter
     - Example tags: Tech, Design, Product, Education, etc.
     - Instructions: "Open any channel page → Edit frontmatter → Add tags in comma-separated format"
   - Section: "Video Discovery"
     - Explain clicking channel links in dashboard or video notes
     - Explain browsing by category in Channels-Index
   - Section: "Adding New Channels"
     - Explain that channels auto-generate when videos are scraped
     - "No manual action needed — system auto-detects new channels"
3. Keep language simple, non-technical

Requirements covered: User-facing documentation for CHAN-02 (categorization)
  </action>
  <verify>
    <automated>
[ -f /Users/avid/Desktop/yt-vault/obsidian-vault/CHANNEL-MANAGEMENT.md ] && \
grep -c "Channel Management\|Categoriz\|Category\|tags" /Users/avid/Desktop/yt-vault/obsidian-vault/CHANNEL-MANAGEMENT.md
    </automated>
  </verify>
  <done>
- File exists at correct path
- Contains guidance on browsing, categorizing, discovering, and auto-adding channels
- Written in user-friendly language
- File is valid markdown
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Channel pages → Video links | Wikilinks are safe (internal, no URL injection) |
| User input → Channel tags | User edits frontmatter directly (text field, no code execution) |
| Dataview queries → Video filtering | Obsidian dataview plugin handles safely; no user input in query strings |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Injection | Dataview queries | Accept | Dataview queries are static; channel_id is generated by scraper (safe), not user input |
| T-03-02 | Tampering | Channel frontmatter | Accept | Users directly edit frontmatter; Obsidian YAML parser validates structure |
| T-03-03 | Disclosure | Channel metadata | Accept | All metadata already in vault locally; no new exposure |
| T-03-04 | Denial of Service | Large video lists per channel | Accept | Largest channel has ~15 videos; Obsidian dataview handles efficiently |

</threat_model>

<verification>
## Overall Phase Checks

1. **CHAN-01 ✓** — Auto-generate one page per unique channel (10 channels → 10 pages)
2. **CHAN-02 ✓** — User can assign tags/categories (tags field in frontmatter, user-editable)
3. **CHAN-03 ✓** — Filter/browse by channel category (Channels-Index.md with category queries)
4. **CHAN-04 ✓** — Channel page displays chronological videos (dataview queries sort by date DESC)

## Final Validation Checklist

- [ ] All 10 channels have dedicated .md pages in `/obsidian-vault/channels/`
- [ ] Each channel page has frontmatter: title, channel_id, type: channel, tags: []
- [ ] Each channel page has YouTube link to creator channel
- [ ] Each channel page has dataview query listing videos sorted by date DESC
- [ ] Channels-Index.md exists with "All Channels" and "Browse by Category" sections
- [ ] At least one channel page has tags assigned (for demonstration)
- [ ] Clicking channel link in video notes → navigates to channel page
- [ ] Clicking channel link in dashboard → navigates to channel page
- [ ] CHANNEL-MANAGEMENT.md user guide exists with categorization instructions
- [ ] No existing channel pages were overwritten (preserves user edits)
- [ ] All files are valid markdown/YAML (no parse errors)

</verification>

<success_criteria>
1. All 10 unique YouTube channels from channels.json have dedicated .md pages
2. Each channel page displays the channel name and YouTube link
3. Each channel page has a tags field for user-assigned categories (initially empty)
4. Each channel page displays all videos from that channel in chronological order (newest first)
5. Channels-Index.md provides overview of all channels and allows filtering by category
6. Users can assign tags to channels by editing frontmatter (no special UI needed)
7. Users can browse videos by channel category via Channels-Index
8. Channel pages auto-generate for new channels as videos are added
9. No videos missing from channel pages (dataview queries are complete)
10. Dashboard and video notes can navigate to channel pages via wikilinks

</success_criteria>

<output>
After completion, create `.planning/phases/03-channel-management/03-03-SUMMARY.md`
</output>
