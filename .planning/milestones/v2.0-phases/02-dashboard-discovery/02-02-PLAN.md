---
phase: 02-dashboard-discovery
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - obsidian-vault/Bases/scrapedVideos.base
  - obsidian-vault/Dashboard.md
autonomous: true
requirements: [DASH-01, DASH-02, DASH-03, DASH-04]
---

<objective>
Build searchable, sortable dashboard using Obsidian Bases for video discovery.

Purpose: Enable users to discover videos across the vault via search, filtering, and navigation to topics and channels.

Output: Enhanced Bases view with search-enabled columns, sortable table, and working navigation links to topic pages and channel pages (Phase 3).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md

This plan operates within an existing Obsidian vault with:
- Video notes stored in `/obsidian-vault/videos/` with YAML frontmatter
- Topic pages auto-generated in `/obsidian-vault/topics/`
- Bases framework configured in `/obsidian-vault/Bases/`
- Phase 1 (Enhanced Templates) already completed with metadata in video notes
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-dashboard-discovery/02-CONTEXT.md

## Video Note Structure

From video frontmatter analysis:
```yaml
---
title: "Video Title"
source: "https://www.youtube.com/watch?v=..."
channel: [[Channel Name]]
channel_id: "YouTube channel ID"
video_id: "YouTube video ID"
topics:
  - "[[Topic 1]]"
  - "[[Topic 2]]"
date: YYYY-MM-DD
type: video
tags:
  - youtube/video
---
```

## Existing Bases Configuration

Current `/obsidian-vault/Bases/scrapedVideos.base`:
- Filters by `file.tags.contains("youtube/video")`
- Table view with columns: file.name, channel, date, topics
- Sorts by date (DESC), then channel (ASC)

## Key Links Required

1. **Channel links** → Will navigate to `/obsidian-vault/channels/{channel-name}.md` (Phase 3 auto-generated)
2. **Topic links** → Already navigate to `/obsidian-vault/topics/{topic-name}.md` (exist from Phase 1)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance Bases configuration for search and filtering</name>
  <files>obsidian-vault/Bases/scrapedVideos.base</files>
  <action>
Update the Bases YAML config to enable full-text search and add filter options:

1. Add `name: "All Videos"` to the table view (for clarity)
2. Ensure all columns are searchable:
   - file.name (title) — implicit in Bases
   - channel — add to order list if missing
   - date — keep for sorting
   - topics — add to order list for filtering by tag
3. Add optional filters:
   - `by-channel`: Filter by channel property (user-selectable in UI)
   - `by-date`: Keep date as sortable column
4. Preserve existing sort order: date DESC, channel ASC

Bases native search will automatically index all columns. The goal is to ensure columns are properly configured for discovery without breaking existing functionality.

Reference DASH-01 (search/filter via Bases) and DASH-02 (sortable table).
  </action>
  <verify>
    <automated>grep -E "name:|type: table|order:|sort:" /Users/avid/Desktop/yt-vault/obsidian-vault/Bases/scrapedVideos.base</automated>
  </verify>
  <done>Bases file has table view with name, all columns in order list, and date+channel sort configured. File valid YAML.</done>
</task>

<task type="auto">
  <name>Task 2: Add topic and channel navigation to Dashboard</name>
  <files>obsidian-vault/Dashboard.md</files>
  <action>
Enhance Dashboard.md to provide clear navigation to the Bases view and explain how to use it for discovery:

1. Replace the current one-liner `![[scrapedVideos.base]]` with:
   - Header: "# Video Discovery Dashboard"
   - Brief description: "Search and browse all videos in your vault"
   - Embed: `![[scrapedVideos.base]]` (keep this)
   - Navigation hints:
     - "Click on **Topics** column to navigate to topic pages"
     - "Click on **Channel** column to navigate to creator pages (Phase 3)"
     - "Use Bases search (top) to find videos by title or channel"
     - "Sort by date, channel, or title using column headers"

2. Keep structure simple — one Bases view, clear instructions.

3. No styling changes; rely on Obsidian theme.

Reference DASH-03 (browse by topic) and DASH-04 (browse by creator).
  </action>
  <verify>
    <automated>grep -E "# Video Discovery|Topics|Channel|search" /Users/avid/Desktop/yt-vault/obsidian-vault/Dashboard.md</automated>
  </verify>
  <done>Dashboard.md contains header, description, Bases embed, and navigation hints for topic/channel linking.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    - Bases configuration with searchable columns (title, channel, date, topics)
    - Sortable table (by date, channel, title)
    - Dashboard.md with navigation instructions
  </what-built>
  <how-to-verify>
    1. Open Obsidian vault at `/Users/avid/Desktop/yt-vault/obsidian-vault/`
    2. Navigate to Dashboard.md
    3. Verify the Bases embed loads with a table of videos
    4. Confirm columns visible: file name (title), channel, date, topics
    5. Test sort: Click column headers to verify sorting works (date DESC default)
    6. Test search: Type in Bases search box (top left) — search should filter by title/channel
    7. Test navigation: Click a topic name → should navigate to topic page
    8. Verify empty state: If no videos, dashboard shows empty table (not error)
    9. Report: "verified" if all checks pass, or describe issues encountered
  </how-to-verify>
  <resume-signal>Type "verified" or describe any issues (e.g., "Bases not loading", "search not working", "links broken")</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input → Bases search | Untrusted user text in search box |
| Dashboard → Topic/Channel links | Obsidian internal links (safe, no external URLs) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Injection | Bases search box | Accept | Obsidian Bases handles search input safely (no code execution) |
| T-02-02 | Disclosure | Video metadata in table | Accept | All data already stored locally in vault; no new exposure |
| T-02-03 | Denial of Service | Large result sets | Accept | Obsidian handles pagination/virtual scrolling; no performance requirements specified |

</threat_model>

<verification>
## Overall Phase Checks

1. **DASH-01 ✓** — Search/filter via Bases search enabled (native feature)
2. **DASH-02 ✓** — Sortable table with columns (title, channel, date, topics) configured
3. **DASH-03 ✓** — Topic navigation via clickable topic links in table
4. **DASH-04 ✓** — Channel navigation prepared (links ready for Phase 3 channel pages)

## Final Validation

- [ ] Bases view loads without errors
- [ ] All videos tagged with `youtube/video` appear in table
- [ ] Search filters videos by title and channel in real-time
- [ ] Columns sortable (verified by clicking headers)
- [ ] Topic links navigate to existing topic pages
- [ ] Channel links prepared for Phase 3 pages (no 404s if pages don't exist yet)
- [ ] Dashboard.md is user-friendly with clear instructions
</verification>

<success_criteria>
1. Dashboard displays all videos in a searchable, sortable table
2. User can filter by title/channel via Bases search (DASH-01)
3. User can sort by date, channel, or title via column headers (DASH-02)
4. Clicking topic name navigates to topic page (DASH-03)
5. Clicking channel name prepares navigation to channel page — works once Phase 3 channel pages exist (DASH-04)
6. No videos missing from table (filter captures all `youtube/video` tagged notes)
7. Empty state handled gracefully (shows empty table if no videos in vault)
</success_criteria>

<output>
After completion, create `.planning/phases/02-dashboard-discovery/02-02-SUMMARY.md`
</output>
